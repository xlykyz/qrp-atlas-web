import type { RuleDraft } from '../types/models';
export const initialRuleDraft:RuleDraft={code:'my_trend_rule',name:'自定义趋势规则',version:'1.0.0',description:'由受控规则编辑器创建。',field:'close',entryOperator:'gt',entryValue:10,exitOperator:'lt',exitValue:9};
export function toDefinition(d:RuleDraft):Record<string,unknown>{return {code:d.code.trim(),name:d.name.trim(),version:d.version.trim(),description:d.description.trim(),strategy_type:'declarative',required_fields:['trade_date','ticker',d.field],required_indicators:[],parameters:{},entry:{left:{source_type:'field',code:d.field},operator:d.entryOperator,right:{source_type:'literal',value:d.entryValue}},exit:{left:{source_type:'field',code:d.field},operator:d.exitOperator,right:{source_type:'literal',value:d.exitValue}}};}
export function validateDraft(d:RuleDraft):string[]{const e:string[]=[];if(!/^[a-z][a-z0-9_]{1,63}$/.test(d.code))e.push('策略代码需以小写字母开头，只含小写字母、数字和下划线。');if(!/^\d+\.\d+\.\d+$/.test(d.version))e.push('版本必须是 x.y.z。');if(!d.name.trim())e.push('名称不能为空。');if(!/^[a-z_][a-z0-9_]*$/.test(d.field))e.push('字段名不合法。');return e;}
