import { describe, expect, it } from 'vitest';
import { createBacktestForm, parseTickers, toCreateTaskRequest, validateBacktestForm } from './form';
import type { Strategy } from '../types/models';
const strategy: Strategy = { code:'dual_sma_trend', name:'双均线趋势', version:'1.0.0', family:'trend', description:'', scope:'asset', strategy_type:'classic', required_fields:[], required_indicators:[], indicator_requests:[], product_supported:true, requires_historical_universe:false, supported_universe_modes:['tickers'], supported_entry_timings:['next_open'], requires_portfolio_config:true, parameter_schema:{ fast_window:{ type:'integer', required:true, default:3, has_default:true, minimum:2, maximum:100, description:null, label:'快线', enum:null }, slow_window:{ type:'integer', required:true, default:8, has_default:true, minimum:3, maximum:300, description:null, label:'慢线', enum:null } } };
describe('backtest form adapter', () => { it('normalizes tickers', () => expect(parseTickers('000001.sz, 600000.SH  000001.SZ')).toEqual(['000001.SZ','600000.SH'])); it('validates date and parameter bounds', () => { const model=createBacktestForm(strategy); model.endDate='2023-01-01'; model.params.fast_window='1'; const errors=validateBacktestForm(model,strategy); expect(errors.endDate).toBeTruthy(); expect(errors['param.fast_window']).toBeTruthy(); }); it('maps the view model to the backend contract', () => { const request=toCreateTaskRequest(createBacktestForm(strategy),strategy); expect(request.strategy_params).toEqual({fast_window:3,slow_window:8}); expect(request.tickers).toEqual(['000001.SZ']); expect(request.position.initial_cash).toBe(1_000_000); }); });
