# 阶段三验证报告：核心产品建设

> 状态：进行中
> 开始日期：2026-07-16
> 前端仓库：`E:\projects\qrp-atlas-web`
> current-main 后端基线：`main@a307e58e138677cadcc38a0da2148639b1f99a5c`

## 1. 执行基线

阶段三严格复用阶段二已经验收的 App Shell、Design Tokens、Design System、页面状态、API Client 与回测黄金工作流。没有引入第二套视觉语言，没有从旧 `qrp-atlas/web/` 迁移 UI、布局、路由或状态架构。

正式代码仍只读取统一的 `VITE_API_BASE_URL`。阶段三真实业务数据浏览器验收使用 `http://192.168.0.102:8000`；本机 `http://127.0.0.1:8001` 继续保留阶段二 current-main + QA 回测环境。前端没有硬编码双 API，也没有请求失败后的 Mock 回退。

## 2. 今日工作台 `/today?date=YYYY-MM-DD`

### 2.1 产品范围

已完成：

- 通过 `GET /api/daily/dates` 确认不晚于当前自然日的最新有效交易日；
- 将研究日写入 URL，并支持切换历史交易日；
- 通过全市场日行情展示上涨、下跌、平盘、有效样本、成交额和涨跌幅中位数；
- 通过独立 `zt_pool` / `dt_pool` 展示涨跌停数量与极端标的，不在前端重算权威池口径；
- 展示主要指数最近可用收盘，并明确指数涨跌幅是相邻两个指数收盘的展示转换；
- 展示人工市场阶段记录；无记录时不通过前端阈值伪造市场阶段；
- 合并近期个股研报与机构调研为“研究证据流”，并标明其不代表 QRP 观点；
- 展示回测任务工作队列；live API 缺少回测路由时显示明确的契约漂移状态；
- 展示 API 健康、数据库大小、关键数据集覆盖日期、行数和相对研究日的新鲜度；
- 所有下钻链接携带交易日或对象上下文。

### 2.2 代码边界

新增独立 domain：

```text
src/domains/today/
  api/
  components/
  hooks/
  lib/
  pages/
  types/
```

页面仅组合查询状态与 domain 组件；DTO、API 调用、日期/广度/指数/新鲜度转换与渲染组件均已下沉。最长页面文件低于 100 行，没有新增巨型页面组件。

### 2.3 真实 API 验收

验收研究日：`2026-07-16`。

浏览器真实返回包括：

- 全市场样本 5,807 只，有效涨跌样本 5,536 只；
- 上涨 2,503、下跌 2,863、平盘 170；
- 涨停池 42、跌停池 31；
- 市场成交额约 2.42 万亿；
- 四个主要指数同日更新；
- 个股研报与机构调研真实记录；
- 数据库约 4.67 GB，关键市场与研究表更新到 `2026-07-16`。

浏览器检查结果：

- 页面自动把 URL 恢复为 `/today?date=2026-07-16`；
- 切换到 `2026-07-15` 后 URL、市场快照时间和“历史研究日”上下文同步更新；
- 1280×720 实际视口无文档级横向溢出；
- 首屏保留研究日、刷新、复盘主动作、市场广度、任务与数据状态；
- 控制台无应用 error / warning；
- 视觉继续使用阶段二浅色、高密度、克制的研究终端语言。

live 部署没有 `/api/backtest/tasks`，因此任务区显示“当前 API 部署未包含回测任务路由”；这属于已记录的 live/main 契约漂移，不使用 Mock 掩盖。

### 2.4 自动化门禁

今日工作台分域提交前：

```text
npm run lint       # 通过
npm run typecheck  # TypeScript strict 通过
npm run test       # 7 files / 17 tests 通过
npm run build      # 通过
```

Vite 仍报告主 chunk 超过 500 kB 的非阻塞警告。阶段三后续路由增多前应按阶段一架构要求引入 route-level lazy import，避免继续扩大首包；该警告不影响当前构建产物运行。

## 3. 市场复盘 `/review/market?date=YYYY-MM-DD`

### 3.1 产品范围

已完成：

- 交易日、极端池方向、行业与代码/名称搜索写入 URL；
- 全市场上涨、下跌、平盘、有效样本、成交额和涨跌幅中位数；
- 四个主要指数收盘、相邻收盘展示涨跌幅和当日开收变化；
- 全市场日行情强弱排序及个股下钻，交易日上下文持续保留；
- 使用后端 `/api/zt-pool`、`/api/dt-pool` 展示完整极端池，不在浏览器重算权威池；
- 对涨跌停池做明确标识的行业分布展示转换；
- 人工阶段记录读取与 `/api/phase` upsert 表单；无记录时不生成或提交伪造判断；
- 全页 loading、empty、error、partial、refreshing 和 success 状态；
- 路由级 lazy import，阶段三新增页面不再扩大单一首包。

页面仅组合查询与 URL 状态，API、DTO、查询、纯转换、表格和阶段编辑均位于独立 `market-review` domain。

### 3.2 真实 API 与浏览器验收

验收交易日：`2026-07-16`，API：`http://192.168.0.102:8000`。

真实返回并展示：

- 全市场 5,807 个样本，有效涨跌样本 5,536 个；
- 上涨 2,503、下跌 2,863、平盘 170；
- 涨停 42、跌停 31，完整明细共 73 条；
- 市场成交额约 2.42 万亿，涨跌幅中位数 -0.18%；
- 上证综指、科创 50、深证成指和创业板指同日更新；
- 涨跌停行业分布、强弱标的及封板/开板字段来自真实数据。

浏览器检查结果：

- `pool=down&industry=半导体&q=华天` 可恢复为唯一“华天科技”结果；
- 切换到 `2026-07-15` 后 URL、市场数据和“历史复盘”上下文同步变化；
- 未保存的阶段草稿不会跨交易日泄漏；
- 个股下钻链接为 `/research/stocks/600664?date=2026-07-16`，保留对象与日期；
- 1280×720 和 800×900 实际浏览器视口均无文档级横向溢出；
- 800px 下主从区改单列，指标带换行，表格仍限制在容器内；
- 浏览器控制台无应用 error / warning；
- 未为验收向真实库写入虚构市场判断；表单已连接 OpenAPI 定义的真实 `POST /api/phase`，待用户形成判断后提交。

### 3.3 自动化门禁

```text
npm run lint       # 通过
npm run typecheck  # TypeScript strict 通过
npm run test       # 8 files / 19 tests 通过
npm run build      # 通过；路由级拆包后无 >500 kB chunk 警告
```

## 4. 后续分域

- 个股研究：待实现；
- 研究记录：待实现；
- 系统数据诊断：待实现。
